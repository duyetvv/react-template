/*
 * ProductNew Messages
 *
 * This contains all the text for the ProductNew container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProductNew';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProductNew container!',
  },
});
